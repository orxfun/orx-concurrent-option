[package]
name = "orx-concurrent-option"
version = "1.5.0"
edition = "2024"
authors = ["orxfun <orx.ugur.arikan@gmail.com>"]
description = "ConcurrentOption is a lock-free concurrent read and write option type."
license = "MIT OR Apache-2.0"
repository = "https://github.com/orxfun/orx-concurrent-option/"
keywords = ["concurrency", "option", "data-structures", "atomic", "lock-free"]
categories = ["data-structures", "concurrency", "rust-patterns", "no-std"]

[dependencies]

[dev-dependencies]
test-case = "3.3.1"

[features]
default = []
